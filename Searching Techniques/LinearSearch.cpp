#include<bits/stdc++.h>
using namespace std;
bool search(int arr[],int key)
{
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]==key)
        {
            return true;
        }
        else{
            return false;
        }
    }
}