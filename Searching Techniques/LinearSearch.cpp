#include<bits/stdc++.h>
using namespace std;
bool search(int arr[],intint key)
{
    for(int i=0;i<n;i++)
    {
        if(arr[i]==key)
        {
            return true;
        }
        else{
            return false;
        }
    }
}